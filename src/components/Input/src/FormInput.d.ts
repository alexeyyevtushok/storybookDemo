export interface FormInputProps {
    withIcon?: boolean
    iconColor?: string,
    placeholder?: string,
    header: string,
    onChange?: () => any
}

export interface IconProps {
    color?: string,
    size?: number
}